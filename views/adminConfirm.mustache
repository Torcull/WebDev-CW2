<html>
{{>header}}
<body class="container text-center">
    <h1>{{title}}</h1>
    <div>
    <div class="text-right">
    </div>
    <div class="text-right" style="margin-bottom: 1em;">

    </div>
        {{#users}}
            <div className="entries">

                {{user}} 
                <br>
                {{role}}
                <p>
                   Are you sure you want to permantely remove this user?
                </p>
            </div>
            </div>
            <hr>
            <a href="/admin" class="btn btn-primary">Cancel</a>
            <a href="/adminConfirm/{{user}}" class="btn btn-primary">Confirm</a>
        {{/users}}
        {{^users}}
            <h1>User not Found</h1>
        {{/users}}

    </div>
</body>
</html>